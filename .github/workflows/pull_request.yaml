name: pull_request

on: [pull_request]

jobs:
  pull_request:
    uses: MapColonies/shared-workflows/.github/workflows/pull_request.yaml@v5.0.1
    secrets: inherit
    with:
      openApiFilePath: './bundledApi.yaml'
